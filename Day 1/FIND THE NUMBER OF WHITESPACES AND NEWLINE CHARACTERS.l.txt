%{
#include<stdio.h>
int whitespaces = 0;
int newlines = 0;
%}

%%
" "  { ++whitespaces; }
"\n" { ++newlines; }
.    { /* ignore */ }

%%

int main(void)
{
    yylex();
    printf("Whitespaces: %d\n", whitespaces);
    printf("Newlines: %d\n", newlines);
    return 0;
}
